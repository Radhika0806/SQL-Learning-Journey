# WHERE Clause

The WHERE clause is used to filter records based on specified conditions.

## Example

### Show countries whose name includes "United"

```sql
SELECT name
FROM world
WHERE name LIKE 'United%';
```
